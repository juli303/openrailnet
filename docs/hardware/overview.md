# Hardware Overview

OpenRailNet defines a general hardware architecture for nodes.

Each node consists of:

- Port A (link to previous vehicle)
- Port B (link to next vehicle)
- Local interface (internal modules)
- Processing unit (MCU)

The hardware architecture is designed to be:
- simple
- modular
- implementable on low-cost hardware